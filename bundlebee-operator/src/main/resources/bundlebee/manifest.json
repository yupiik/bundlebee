{
  "$schema": "https://raw.githubusercontent.com/yupiik/bundlebee/gh-pages/generated/jsonschema/manifest.descriptor.json",
  "alveoli": [
    {
      "name": "${project.groupId}:bundlebee-operator:${project.version}",
      "descriptors": [
        {
          "name": "${project.groupId}/bundlebee-operator/crd.json",
          "interpolate": true
        },
        {
          "name": "${project.groupId}/bundlebee-operator/role.json",
          "interpolate": true
        },
        {
          "name": "${project.groupId}/bundlebee-operator/rolebinding.json",
          "interpolate": true
        },
        {
          "name": "${project.groupId}/bundlebee-operator/serviceaccount.json",
          "interpolate": true
        },
        {
          "name": "${project.groupId}/bundlebee-operator/token.json"
        },
        {
          "name": "${project.groupId}/bundlebee-operator/deployment.json",
          "interpolate": true
        }
      ]
    }
  ]
}